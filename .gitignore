.env
data/*.db
# SQLite WAL sidecars, created since db.py enables journal_mode = WAL
data/*.db-shm
data/*.db-wal
__pycache__/
*.pyc
.venv/
venv/
*.service
# ...except the beta auto-update unit, which docs/testing.md Part 1e installs
# straight from this checkout.
!deploy/*.service

# Hand-maintained guild/user/channel display-name lookup for the ops
# dashboard (web/directory.py) - not secret, just personal to one operator's
# servers. web/directory.example.json is the tracked template.
web/directory.json